import { defineMessages } from 'react-intl';

export default defineMessages({
  membership: {
    id: 'containers.AboutPage.Membership',
    defaultMessage: 'The Mormon Transhumanist Association has {memberCount} members in {countryCount} countries.',
  },
});
